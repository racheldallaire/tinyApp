<!DOCTYPE html>
<html lang="en">
<head>
  <% include ./partials/_header %>
</head>
<body>
    <div class="table">
      <b>Transformed URLs:</b>
      <ul style="list-style-type:circle">
      <% for (var key in urls) { %>
      <li><%= key + ": " + urls[key] + " " %><a href="/urls/<%= key %>">Shorten</a></li>
      <% } %><p>
        <a href="/urls/new">This is a form</a>
      </p>
    </ul>
    </div>
    </p>
</body>
</html>